{
  "buildCommand": "cd renderer && ../node_modules/.bin/next build",
  "outputDirectory": "renderer/.next",
  "installCommand": "yarn install",
  "devCommand": "cd renderer && ../node_modules/.bin/next dev",
  "framework": "nextjs"
}
